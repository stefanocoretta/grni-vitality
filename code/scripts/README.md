This folder contains scripts that are sourced by the .qmd files in `code/`.

The `packages.R` file shall be sourced in each .qmd file. Each new package used in the project shall be added to `packages.R`.
